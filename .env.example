# URL do backend Railway (OBRIGATÓRIO para sincronização)
VITE_API_BASE_URL=https://sistema-protocolos-juridicos-production.up.railway.app

# PostgreSQL Database URL (Railway irá fornecer automaticamente)
DATABASE_URL=postgresql://username:password@host:port/database

# Configurações PostgreSQL locais (apenas se não usar Railway)
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=protocolos_juridicos