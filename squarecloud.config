# Configuração Square Cloud - Sistema Jurídico Completo
# Frontend React + Backend Node.js no mesmo domínio

MAIN=server/server.js
MEMORY=512
VERSION=recommended

# Variáveis de ambiente para Square Cloud
NODE_ENV=production
PORT=80
HOST=0.0.0.0